<Window x:Class="AiDataGateway.LocalLogViewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        xmlns:models="clr-namespace:AiDataGateway.LocalLogViewer.Models"
        xmlns:viewModels="clr-namespace:AiDataGateway.LocalLogViewer.ViewModels"
        Title="本地日志查看器" Height="900" Width="1480" MinHeight="700" MinWidth="1080"
        WindowStartupLocation="CenterScreen" WindowStyle="None" ResizeMode="CanResize" Background="{StaticResource PageBrush}" Icon="Assets/gateway-app.ico">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="40" ResizeBorderThickness="5" GlassFrameThickness="0" CornerRadius="0" UseAeroCaptionButtons="False" />
    </shell:WindowChrome.WindowChrome>
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolVisibility" />
        <viewModels:ZeroCountToVisibilityConverter x:Key="ZeroCountVisibility" />
        <Style x:Key="Label" TargetType="TextBlock"><Setter Property="Foreground" Value="{StaticResource RegularTextBrush}"/><Setter Property="FontSize" Value="12"/><Setter Property="Margin" Value="0,4,0,0"/></Style>
        <Style x:Key="PageTitle" TargetType="TextBlock"><Setter Property="FontSize" Value="20"/><Setter Property="FontWeight" Value="SemiBold"/><Setter Property="Foreground" Value="{StaticResource TextBrush}"/></Style>
        <Style x:Key="SectionTitle" TargetType="TextBlock"><Setter Property="FontSize" Value="15"/><Setter Property="FontWeight" Value="SemiBold"/><Setter Property="Foreground" Value="{StaticResource TextBrush}"/></Style>
        <Style x:Key="Card" TargetType="Border"><Setter Property="Background" Value="White"/><Setter Property="BorderBrush" Value="{StaticResource LightBorderBrush}"/><Setter Property="BorderThickness" Value="1"/><Setter Property="CornerRadius" Value="8"/><Setter Property="Effect" Value="{StaticResource CardShadow}"/></Style>
        <Style x:Key="NavButton" TargetType="RadioButton">
            <Setter Property="Height" Value="46"/><Setter Property="Margin" Value="12,3"/><Setter Property="Padding" Value="14,0"/><Setter Property="Cursor" Value="Hand"/><Setter Property="Foreground" Value="{StaticResource RegularTextBrush}"/><Setter Property="FontSize" Value="13"/><Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="RadioButton"><Grid><Border x:Name="NavBackground" Background="Transparent" CornerRadius="6" Padding="{TemplateBinding Padding}"><Grid><Grid.ColumnDefinitions><ColumnDefinition Width="28"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><TextBlock x:Name="NavDot" Text="●" Foreground="#C0C4CC" FontSize="11" VerticalAlignment="Center"/><ContentPresenter Grid.Column="1" VerticalAlignment="Center"/></Grid></Border><Border x:Name="NavAccent" Width="3" CornerRadius="1.5" Background="Transparent" HorizontalAlignment="Left" Margin="0,8"/></Grid><ControlTemplate.Triggers><Trigger Property="IsMouseOver" Value="True"><Setter TargetName="NavBackground" Property="Background" Value="#F5F7FA"/><Setter Property="Foreground" Value="{StaticResource PrimaryBrush}"/></Trigger><Trigger Property="IsChecked" Value="True"><Setter TargetName="NavBackground" Property="Background" Value="{StaticResource PrimarySoftBrush}"/><Setter TargetName="NavAccent" Property="Background" Value="{StaticResource PrimaryBrush}"/><Setter TargetName="NavDot" Property="Foreground" Value="{StaticResource PrimaryBrush}"/><Setter Property="Foreground" Value="{StaticResource PrimaryBrush}"/><Setter Property="FontWeight" Value="SemiBold"/></Trigger></ControlTemplate.Triggers></ControlTemplate></Setter.Value></Setter>
        </Style>
        <Style x:Key="LevelBadge" TargetType="Border">
            <Setter Property="Background" Value="#ECF5FF"/><Setter Property="BorderBrush" Value="#B3D8FF"/><Setter Property="BorderThickness" Value="1"/><Setter Property="CornerRadius" Value="4"/><Setter Property="Padding" Value="7,2"/><Setter Property="HorizontalAlignment" Value="Left"/>
            <Style.Triggers><DataTrigger Binding="{Binding Level}" Value="Error"><Setter Property="Background" Value="#FEF0F0"/><Setter Property="BorderBrush" Value="#FAB6B6"/></DataTrigger><DataTrigger Binding="{Binding Level}" Value="Fatal"><Setter Property="Background" Value="#FDE2E2"/><Setter Property="BorderBrush" Value="#F56C6C"/></DataTrigger><DataTrigger Binding="{Binding Level}" Value="Warn"><Setter Property="Background" Value="#FDF6EC"/><Setter Property="BorderBrush" Value="#F5DAB1"/></DataTrigger><DataTrigger Binding="{Binding Level}" Value="Warning"><Setter Property="Background" Value="#FDF6EC"/><Setter Property="BorderBrush" Value="#F5DAB1"/></DataTrigger><DataTrigger Binding="{Binding Level}" Value="Debug"><Setter Property="Background" Value="#F4F4F5"/><Setter Property="BorderBrush" Value="#D3D4D6"/></DataTrigger></Style.Triggers>
        </Style>
        <HierarchicalDataTemplate DataType="{x:Type models:StructuredNode}" ItemsSource="{Binding Children}"><StackPanel Orientation="Horizontal" Margin="1,4"><TextBlock Text="{Binding Name}" FontWeight="SemiBold" Foreground="#3A4A5B"/><TextBlock Text="  "/><TextBlock Text="{Binding Value}" Foreground="{StaticResource SecondaryTextBrush}" TextTrimming="CharacterEllipsis" MaxWidth="430"/></StackPanel></HierarchicalDataTemplate>
        <SolidColorBrush x:Key="TitleBarBrush" Color="White" />
        <Style x:Key="CaptionButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="44" />
            <Setter Property="Height" Value="40" />
            <Setter Property="FontFamily" Value="Segoe MDL2 Assets" />
            <Setter Property="FontSize" Value="10" />
            <Setter Property="Foreground" Value="#5A6B7F" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Focusable" Value="False" />
            <Setter Property="shell:WindowChrome.IsHitTestVisibleInChrome" Value="True" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="HoverHost" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="HoverHost" Property="Background" Value="#F0F2F6" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="HoverHost" Property="Background" Value="#E2E7EE" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="CloseCaptionButtonStyle" BasedOn="{StaticResource CaptionButtonStyle}" TargetType="Button">
            <Setter Property="Foreground" Value="#5A6B7F" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E81123" />
                    <Setter Property="Foreground" Value="White" />
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#C40E1D" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid x:Name="RootLayout">
        <Grid.ColumnDefinitions><ColumnDefinition Width="216"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
        <Grid.RowDefinitions><RowDefinition Height="40"/><RowDefinition Height="64"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="34"/></Grid.RowDefinitions>

        <Border Grid.ColumnSpan="2" Grid.Row="0" Panel.ZIndex="10" Background="{StaticResource TitleBarBrush}" BorderBrush="{StaticResource LightBorderBrush}" BorderThickness="0,0,0,1">
            <DockPanel LastChildFill="True">
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                    <Button x:Name="MinimizeButton" Style="{StaticResource CaptionButtonStyle}" Content="&#xE921;" Click="OnMinimizeClick" />
                    <Button x:Name="MaximizeButton" Style="{StaticResource CaptionButtonStyle}" Content="&#xE922;" Click="OnMaximizeClick" />
                    <Button Style="{StaticResource CloseCaptionButtonStyle}" Content="&#xE8BB;" Click="OnCloseClick" />
                </StackPanel>
                <TextBlock DockPanel.Dock="Left" Text="本地日志查看器" FontSize="13" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}" VerticalAlignment="Center" Margin="14,0,0,0"/>
                <TextBlock DockPanel.Dock="Left" Text="LOCAL LOG VIEWER" Margin="10,1,0,0" FontSize="9.5" Foreground="#9AA7B5" VerticalAlignment="Center"/>
            </DockPanel>
        </Border>

        <Border Grid.RowSpan="5" Background="White" BorderBrush="{StaticResource LightBorderBrush}" BorderThickness="0,0,1,0">
            <Grid>
                <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
                <StackPanel Grid.Row="0" Margin="0,56,0,0">
                    <TextBlock Text="工作台" Foreground="{StaticResource SecondaryTextBrush}" FontSize="11" Margin="22,0,0,7"/>
                    <RadioButton Content="应用日志" Style="{StaticResource NavButton}" GroupName="Navigation" IsChecked="{Binding IsLogsNavigationActive, Mode=OneWay}" Command="{Binding NavigateLogsCommand}"/>
                </StackPanel>
                <StackPanel Grid.Row="1" Margin="0,14,0,0">
                    <TextBlock Text="配置" Foreground="{StaticResource SecondaryTextBrush}" FontSize="11" Margin="22,0,0,7"/>
                    <RadioButton Content="日志源配置" Style="{StaticResource NavButton}" GroupName="Navigation" IsChecked="{Binding IsLogConfigurationPage, Mode=OneWay}" Command="{Binding NavigateLogConfigurationCommand}"/>
                    <RadioButton Content="数据库配置" Style="{StaticResource NavButton}" GroupName="Navigation" IsChecked="{Binding IsDatabaseConfigurationPage, Mode=OneWay}" Command="{Binding NavigateDatabaseConfigurationCommand}"/>
                </StackPanel>
                <Border Grid.Row="3" Margin="14,0,14,16" Background="#F0F9EB" BorderBrush="#E1F3D8" BorderThickness="1" CornerRadius="7" Padding="11"><StackPanel><StackPanel Orientation="Horizontal"><Ellipse Width="7" Height="7" Fill="{StaticResource SuccessBrush}" Margin="0,0,7,0"/><TextBlock Text="本机安全模式" Foreground="#529B2E" FontSize="11" FontWeight="SemiBold"/></StackPanel><TextBlock Text="不提供 Web API 或 MCP" Foreground="#85A77A" FontSize="9" Margin="14,4,0,0"/></StackPanel></Border>
            </Grid>
        </Border>

        <Grid Grid.Column="1" Grid.Row="1" Background="White">
            <Border BorderBrush="{StaticResource LightBorderBrush}" BorderThickness="0,0,0,1"><Border.Effect><DropShadowEffect BlurRadius="8" ShadowDepth="1" Opacity="0.06" Color="#17233D"/></Border.Effect></Border>
            <Border Width="3" HorizontalAlignment="Left" Background="{StaticResource PrimaryBrush}"/>
            <Grid Margin="25,0"><StackPanel VerticalAlignment="Center"><TextBlock Text="应用日志" Style="{StaticResource PageTitle}" Visibility="{Binding IsLogsPage, Converter={StaticResource BoolVisibility}}"/><TextBlock Text="日志详情" Style="{StaticResource PageTitle}" Visibility="{Binding IsLogDetailPage, Converter={StaticResource BoolVisibility}}"/><TextBlock Text="SQL 分析" Style="{StaticResource PageTitle}" Visibility="{Binding IsLogSqlPage, Converter={StaticResource BoolVisibility}}"/><TextBlock Text="日志源配置" Style="{StaticResource PageTitle}" Visibility="{Binding IsLogConfigurationPage, Converter={StaticResource BoolVisibility}}"/><TextBlock Text="数据库配置" Style="{StaticResource PageTitle}" Visibility="{Binding IsDatabaseConfigurationPage, Converter={StaticResource BoolVisibility}}"/></StackPanel><Border HorizontalAlignment="Right" VerticalAlignment="Center" Background="#F0F9EB" BorderBrush="#C2E7B0" BorderThickness="1" CornerRadius="12" Padding="10,4"><StackPanel Orientation="Horizontal"><Ellipse Width="7" Height="7" Fill="{StaticResource SuccessBrush}" Margin="0,0,6,0"/><TextBlock Text="本机模式" Foreground="#529B2E" FontSize="11"/></StackPanel></Border></Grid>
        </Grid>

        <Border Grid.Column="1" Grid.Row="2" Height="48" Background="White" BorderBrush="{StaticResource LightBorderBrush}" BorderThickness="0,0,0,1" Visibility="{Binding IsLogsNavigationActive, Converter={StaticResource BoolVisibility}}">
            <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="220"/></Grid.ColumnDefinitions>
            <ListBox x:Name="WorkspaceTabStrip" ItemsSource="{Binding WorkspaceTabs}" SelectedItem="{Binding SelectedWorkspaceTab}" SelectionChanged="WorkspaceTabStrip_SelectionChanged" Loaded="WorkspaceTabStrip_Loaded" Background="Transparent" BorderThickness="0" Padding="14,6"
                     ScrollViewer.HorizontalScrollBarVisibility="Hidden" ScrollViewer.VerticalScrollBarVisibility="Disabled">
                <ListBox.ItemsPanel><ItemsPanelTemplate><StackPanel Orientation="Horizontal"/></ItemsPanelTemplate></ListBox.ItemsPanel>
                <ListBox.ItemContainerStyle><Style TargetType="ListBoxItem"><Setter Property="Padding" Value="0"/><Setter Property="Margin" Value="2,0"/><Setter Property="Cursor" Value="Hand"/><Setter Property="Template"><Setter.Value><ControlTemplate TargetType="ListBoxItem"><Border x:Name="TabSurface" Background="Transparent" BorderBrush="Transparent" BorderThickness="1" CornerRadius="6" Padding="12,6"><ContentPresenter/></Border><ControlTemplate.Triggers><Trigger Property="IsMouseOver" Value="True"><Setter TargetName="TabSurface" Property="Background" Value="#F5F7FA"/></Trigger><Trigger Property="IsSelected" Value="True"><Setter TargetName="TabSurface" Property="Background" Value="{StaticResource PrimarySoftBrush}"/><Setter TargetName="TabSurface" Property="BorderBrush" Value="#B3D8FF"/><Setter Property="Foreground" Value="{StaticResource PrimaryBrush}"/></Trigger></ControlTemplate.Triggers></ControlTemplate></Setter.Value></Setter></Style></ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate><DataTemplate><StackPanel Orientation="Horizontal"><TextBlock Text="{Binding Title}" VerticalAlignment="Center" MaxWidth="235" TextTrimming="CharacterEllipsis"/><Button Content="↗" Click="DetachWorkspace_Click" Style="{StaticResource TextButton}" Padding="4,0" MinHeight="20" Margin="8,0,0,0" FontSize="13" Foreground="{StaticResource PrimaryBrush}" Visibility="{Binding CanClose, Converter={StaticResource BoolVisibility}}"/><Button Content="×" Command="{Binding DataContext.CloseWorkspaceTabCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding}" Style="{StaticResource TextButton}" Padding="4,0" MinHeight="20" Margin="2,0,-4,0" FontSize="15" Visibility="{Binding CanClose, Converter={StaticResource BoolVisibility}}"/></StackPanel></DataTemplate></ListBox.ItemTemplate>
            </ListBox>
            <ComboBox Grid.Column="1" ItemsSource="{Binding WorkspaceTabs}" SelectedItem="{Binding SelectedWorkspaceTab}" DisplayMemberPath="Title" Margin="6,6,14,6" ToolTip="全部已打开标签页"/>
            </Grid>
        </Border>

        <!-- 应用日志 -->
        <Grid Grid.Column="1" Grid.Row="3" Margin="20" Visibility="{Binding IsLogsPage, Converter={StaticResource BoolVisibility}}">
            <Grid.RowDefinitions><RowDefinition Height="170" MinHeight="150"/><RowDefinition Height="14"/><RowDefinition Height="*" MinHeight="180"/></Grid.RowDefinitions>
            <Border Style="{StaticResource Card}" Padding="16,8">
                <Grid>
                    <Grid.RowDefinitions><RowDefinition Height="*"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                    <Grid.ColumnDefinitions><ColumnDefinition Width="150"/><ColumnDefinition Width="160"/><ColumnDefinition Width="160"/><ColumnDefinition Width="140"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                    <StackPanel><TextBlock Style="{StaticResource Label}" Text="时间范围"/><ComboBox ItemsSource="{Binding RecentRanges}" SelectedItem="{Binding SelectedRecentRange}" Margin="0,5,10,0"/></StackPanel>
                    <StackPanel Grid.Column="1"><TextBlock Style="{StaticResource Label}" Text="开始日期"/><DatePicker SelectedDate="{Binding FromDate}" IsEnabled="{Binding IsCustomDateRange}" Margin="0,5,10,0"/></StackPanel>
                    <StackPanel Grid.Column="2"><TextBlock Style="{StaticResource Label}" Text="结束日期"/><DatePicker SelectedDate="{Binding ToDate}" IsEnabled="{Binding IsCustomDateRange}" Margin="0,5,10,0"/></StackPanel>
                    <StackPanel Grid.Column="3"><TextBlock Style="{StaticResource Label}" Text="日志级别"/><ComboBox ItemsSource="{Binding Levels}" SelectedItem="{Binding Level}" Margin="0,5,10,0"/></StackPanel>
                    <StackPanel Grid.Column="4">
                        <DockPanel><TextBlock Style="{StaticResource Label}" Text="打印函数 / 调用位置"/><TextBlock Text="{Binding FunctionFilterSummary}" HorizontalAlignment="Right" Foreground="{StaticResource SecondaryTextBrush}" FontSize="10" Margin="0,5,8,0"/></DockPanel>
                        <ComboBox ItemsSource="{Binding FunctionNames}" SelectedItem="{Binding SelectedFunction}" MaxDropDownHeight="280" Margin="0,5,0,0"/>
                    </StackPanel>
                    <Grid Grid.Row="1" Grid.ColumnSpan="5"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><StackPanel Margin="0,0,12,0"><TextBlock Style="{StaticResource Label}" Text="搜索消息、异常或结构化字段"/><TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,0"/></StackPanel><StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Bottom"><Button Content="查询日志" Command="{Binding QueryCommand}" Style="{StaticResource PrimaryButton}" IsDefault="True"/><Button Content="取消" Command="{Binding CancelCommand}" Margin="0"/></StackPanel></Grid>
                </Grid>
            </Border>

            <GridSplitter Grid.Row="1" Height="14" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" ResizeDirection="Rows" ResizeBehavior="PreviousAndNext" Background="Transparent" Cursor="SizeNS"><GridSplitter.Template><ControlTemplate TargetType="GridSplitter"><Border Background="Transparent"><Border Width="46" Height="4" CornerRadius="2" Background="#D8DEE8" HorizontalAlignment="Center" VerticalAlignment="Center"/></Border></ControlTemplate></GridSplitter.Template></GridSplitter>

            <Border Grid.Row="2" Style="{StaticResource Card}" Padding="0">
                <Grid>
                    <Grid.RowDefinitions><RowDefinition Height="56"/><RowDefinition Height="42"/><RowDefinition Height="3"/><RowDefinition Height="*"/><RowDefinition Height="50"/></Grid.RowDefinitions>
                    <Grid Margin="16,0"><StackPanel VerticalAlignment="Center"><TextBlock Text="日志记录" Style="{StaticResource SectionTitle}"/><TextBlock Text="识别到 SQL 时可进入独立分析页面" Foreground="{StaticResource SecondaryTextBrush}" FontSize="10" Margin="0,3,0,0"/></StackPanel><StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center"><Border Background="#ECF5FF" CornerRadius="11" Padding="9,4" Margin="0,0,8,0"><TextBlock Text="{Binding ActiveLogSourceSummary}" Foreground="#337ECC" FontSize="10"/></Border><Border Background="#F4F4F5" CornerRadius="11" Padding="9,4"><TextBlock Text="{Binding PageSummary}" Foreground="{StaticResource SecondaryTextBrush}" FontSize="10"/></Border></StackPanel></Grid>
                    <Border Grid.Row="1" Background="#FAFAFC" BorderBrush="{StaticResource LightBorderBrush}" BorderThickness="0,1,0,1"><Grid Margin="16,0"><Grid.ColumnDefinitions><ColumnDefinition Width="160"/><ColumnDefinition Width="80"/><ColumnDefinition Width="*"/><ColumnDefinition Width="190"/><ColumnDefinition Width="110"/><ColumnDefinition Width="100"/></Grid.ColumnDefinitions><TextBlock Text="时间" VerticalAlignment="Center" Foreground="{StaticResource SecondaryTextBrush}" FontWeight="SemiBold" FontSize="11"/><TextBlock Grid.Column="1" Text="级别" VerticalAlignment="Center" Foreground="{StaticResource SecondaryTextBrush}" FontWeight="SemiBold" FontSize="11"/><TextBlock Grid.Column="2" Text="消息" VerticalAlignment="Center" Foreground="{StaticResource SecondaryTextBrush}" FontWeight="SemiBold" FontSize="11"/><TextBlock Grid.Column="3" Text="打印函数 / 调用位置" VerticalAlignment="Center" Foreground="{StaticResource SecondaryTextBrush}" FontWeight="SemiBold" FontSize="11"/><TextBlock Grid.Column="4" Text="文件" VerticalAlignment="Center" Foreground="{StaticResource SecondaryTextBrush}" FontWeight="SemiBold" FontSize="11"/><TextBlock Grid.Column="5" Text="操作" VerticalAlignment="Center" Foreground="{StaticResource SecondaryTextBrush}" FontWeight="SemiBold" FontSize="11"/></Grid></Border>
                    <ProgressBar Grid.Row="2" IsIndeterminate="True" Visibility="{Binding IsBusy, Converter={StaticResource BoolVisibility}}" Height="3" BorderThickness="0" Foreground="{StaticResource PrimaryBrush}"/>
                    <Grid Grid.Row="3">
                        <ListBox ItemsSource="{Binding LogEntries}" SelectedItem="{Binding SelectedLogEntry}" BorderThickness="0" Background="White" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                            <ListBox.ItemContainerStyle><Style TargetType="ListBoxItem" BasedOn="{StaticResource {x:Type ListBoxItem}}"><Setter Property="Padding" Value="0"/><Setter Property="Margin" Value="0"/></Style></ListBox.ItemContainerStyle>
                            <ListBox.ItemTemplate><DataTemplate><Border Height="49" BorderBrush="{StaticResource LightBorderBrush}" BorderThickness="0,0,0,1" Padding="16,0"><Grid><Grid.ColumnDefinitions><ColumnDefinition Width="160"/><ColumnDefinition Width="80"/><ColumnDefinition Width="*"/><ColumnDefinition Width="190"/><ColumnDefinition Width="110"/><ColumnDefinition Width="100"/></Grid.ColumnDefinitions><TextBlock Text="{Binding DisplayTime}" VerticalAlignment="Center" Foreground="{StaticResource RegularTextBrush}" FontFamily="Consolas" FontSize="11"/><Border Grid.Column="1" Style="{StaticResource LevelBadge}" VerticalAlignment="Center"><TextBlock Text="{Binding Level}" FontSize="10"/></Border><TextBlock Grid.Column="2" Text="{Binding Message}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" Foreground="{StaticResource TextBrush}" Margin="0,0,16,0"/><TextBlock Grid.Column="3" Text="{Binding FunctionName}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" Foreground="#337ECC" FontFamily="Consolas" FontSize="10" Margin="0,0,14,0"/><TextBlock Grid.Column="4" Text="{Binding FileName}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" Foreground="{StaticResource SecondaryTextBrush}" FontSize="11" Margin="0,0,12,0"/><StackPanel Grid.Column="5" Orientation="Horizontal"><Button Content="SQL" Style="{StaticResource TextButton}" Foreground="#E6A23C" Visibility="{Binding HasSql, Converter={StaticResource BoolVisibility}}" Command="{Binding DataContext.OpenLogSqlCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding}"/><Button Content="详情" Style="{StaticResource TextButton}" Command="{Binding DataContext.OpenLogDetailCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding}" Margin="0"/></StackPanel></Grid></Border></DataTemplate></ListBox.ItemTemplate>
                        </ListBox>
                        <StackPanel IsHitTestVisible="False" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="{Binding LogEntries.Count, Converter={StaticResource ZeroCountVisibility}}"><Border Width="58" Height="58" CornerRadius="29" Background="#F5F7FA" HorizontalAlignment="Center"><TextBlock Text="≡" FontSize="28" Foreground="#C0C4CC" HorizontalAlignment="Center" VerticalAlignment="Center"/></Border><TextBlock Text="暂无日志数据" Foreground="{StaticResource SecondaryTextBrush}" HorizontalAlignment="Center" Margin="0,12,0,0"/><TextBlock Text="请先在日志源配置中添加目录，然后点击查询" Foreground="#C0C4CC" FontSize="10" HorizontalAlignment="Center" Margin="0,5,0,0"/></StackPanel>
                    </Grid>
                    <Grid Grid.Row="4" Margin="16,0"><TextBlock Text="每页显示 100 条" Foreground="{StaticResource SecondaryTextBrush}" FontSize="10" VerticalAlignment="Center"/><StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center"><Button Content="上一页" Command="{Binding PreviousPageCommand}"/><Button Content="下一页" Command="{Binding NextPageCommand}" Margin="0"/></StackPanel></Grid>
                </Grid>
            </Border>
        </Grid>

        <!-- 日志详情子页面 -->
        <Grid Grid.Column="1" Grid.Row="3" Margin="20" DataContext="{Binding SelectedWorkspaceTab}" Visibility="{Binding IsLogDetail, Converter={StaticResource BoolVisibility}}">
            <Grid.RowDefinitions><RowDefinition Height="36"/><RowDefinition Height="10"/><RowDefinition Height="*"/></Grid.RowDefinitions>
            <Grid><TextBlock Text="通过上方标签快速切换，点击 ↗ 可在独立窗口中查看" Foreground="{StaticResource SecondaryTextBrush}" FontSize="10" VerticalAlignment="Center"/><StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center"><TextBlock Text="{Binding Entry.DisplayTime}" Foreground="{StaticResource SecondaryTextBrush}" Margin="0,0,12,0"/><Border Style="{StaticResource LevelBadge}" DataContext="{Binding Entry}"><TextBlock Text="{Binding Level}"/></Border></StackPanel></Grid>
            <Grid Grid.Row="2"><Grid.ColumnDefinitions><ColumnDefinition Width="0.9*" MinWidth="260"/><ColumnDefinition Width="14"/><ColumnDefinition Width="1.4*" MinWidth="360"/></Grid.ColumnDefinitions>
                <Border Style="{StaticResource Card}" Padding="0"><Grid><Grid.RowDefinitions><RowDefinition Height="56"/><RowDefinition Height="*"/></Grid.RowDefinitions><Border BorderBrush="{StaticResource LightBorderBrush}" BorderThickness="0,0,0,1" Padding="16,0"><TextBlock Text="结构化字段" Style="{StaticResource SectionTitle}" VerticalAlignment="Center"/></Border><TreeView Grid.Row="1" ItemsSource="{Binding StructuredNodes}" BorderThickness="0" Padding="12" Background="White"/></Grid></Border>
                <GridSplitter Grid.Column="1" Width="14" HorizontalAlignment="Stretch" ResizeDirection="Columns" ResizeBehavior="PreviousAndNext" Background="Transparent" Cursor="SizeWE"><GridSplitter.Template><ControlTemplate TargetType="GridSplitter"><Border Background="Transparent"><Border Width="4" Height="42" CornerRadius="2" Background="#D8DEE8" HorizontalAlignment="Center" VerticalAlignment="Center"/></Border></ControlTemplate></GridSplitter.Template></GridSplitter>
                <Border Grid.Column="2" Style="{StaticResource Card}" Padding="0"><Grid><Grid.RowDefinitions><RowDefinition Height="56"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions><Border BorderBrush="{StaticResource LightBorderBrush}" BorderThickness="0,0,0,1" Padding="16,0"><StackPanel VerticalAlignment="Center"><TextBlock Text="原始日志" Style="{StaticResource SectionTitle}"/><TextBlock Text="{Binding Entry.FilePath}" TextTrimming="CharacterEllipsis" Foreground="{StaticResource SecondaryTextBrush}" FontSize="9" Margin="0,3,0,0"/></StackPanel></Border><Border Grid.Row="1" Background="#F5F7FA" CornerRadius="5" Padding="12" Margin="14,14,14,0"><TextBlock Text="{Binding Entry.Message}" TextWrapping="Wrap" Foreground="{StaticResource TextBrush}"/></Border><TextBox Grid.Row="2" Text="{Binding Entry.RawText}" IsReadOnly="True" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" FontFamily="Consolas" FontSize="12" VerticalContentAlignment="Top" Margin="14"/></Grid></Border>
            </Grid>
        </Grid>

        <!-- SQL 分析子页面 -->
        <Grid Grid.Column="1" Grid.Row="3" Margin="20" DataContext="{Binding SelectedWorkspaceTab}" Visibility="{Binding IsSql, Converter={StaticResource BoolVisibility}}">
            <Grid.RowDefinitions><RowDefinition Height="36"/><RowDefinition Height="10"/><RowDefinition Height="2*" MinHeight="220"/><RowDefinition Height="14"/><RowDefinition Height="*" MinHeight="140"/></Grid.RowDefinitions>
            <Grid>
                <TextBlock Text="点击上方 SQL 标签中的 ↗ 可在独立窗口中查看" Foreground="{StaticResource SecondaryTextBrush}" FontSize="10" VerticalAlignment="Center"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Border CornerRadius="11" Padding="10,4" Margin="0,0,8,0"><Border.Style><Style TargetType="Border"><Setter Property="Background" Value="#FDF6EC"/><Setter Property="BorderBrush" Value="#F5DAB1"/><Setter Property="BorderThickness" Value="1"/><Style.Triggers><DataTrigger Binding="{Binding Entry.SqlIsReadOnly}" Value="True"><Setter Property="Background" Value="#F0F9EB"/><Setter Property="BorderBrush" Value="#C2E7B0"/></DataTrigger></Style.Triggers></Style></Border.Style><TextBlock FontSize="10"><TextBlock.Style><Style TargetType="TextBlock"><Setter Property="Text" Value="仅格式化，禁止执行"/><Setter Property="Foreground" Value="#B88230"/><Style.Triggers><DataTrigger Binding="{Binding Entry.SqlIsReadOnly}" Value="True"><Setter Property="Text" Value="只读 SQL"/><Setter Property="Foreground" Value="#529B2E"/></DataTrigger></Style.Triggers></Style></TextBlock.Style></TextBlock></Border>
                    <Border Background="#F4F4F5" CornerRadius="11" Padding="10,4"><TextBlock Text="{Binding SqlPlaceholderCount, StringFormat=参数占位符 · {0}}" Foreground="{StaticResource SecondaryTextBrush}" FontSize="10"/></Border>
                </StackPanel>
            </Grid>
            <Grid Grid.Row="2"><Grid.ColumnDefinitions><ColumnDefinition Width="1.45*"/><ColumnDefinition Width="14"/><ColumnDefinition Width="0.75*"/></Grid.ColumnDefinitions>
                <Border Style="{StaticResource Card}" Padding="0"><Grid><Grid.RowDefinitions><RowDefinition Height="54"/><RowDefinition Height="*"/></Grid.RowDefinitions><Border BorderBrush="{StaticResource LightBorderBrush}" BorderThickness="0,0,0,1" Padding="16,0"><StackPanel VerticalAlignment="Center"><TextBlock Text="格式化 SQL" Style="{StaticResource SectionTitle}"/><TextBlock Text="从结构化属性或日志消息中自动提取" Foreground="{StaticResource SecondaryTextBrush}" FontSize="9" Margin="0,3,0,0"/></StackPanel></Border><TextBox Grid.Row="1" Text="{Binding Entry.FormattedSql}" IsReadOnly="True" AcceptsReturn="True" TextWrapping="NoWrap" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" FontFamily="Consolas" FontSize="12" VerticalContentAlignment="Top" Margin="14"/></Grid></Border>
                <Border Grid.Column="2" Style="{StaticResource Card}" Padding="0">
                    <Grid>
                        <Grid.RowDefinitions><RowDefinition Height="54"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                        <Border BorderBrush="{StaticResource LightBorderBrush}" BorderThickness="0,0,0,1" Padding="16,0"><TextBlock Text="只读试查" Style="{StaticResource SectionTitle}" VerticalAlignment="Center"/></Border>
                        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                            <StackPanel Margin="16,12">
                                <TextBlock Style="{StaticResource Label}" Text="日志源关联数据库"/>
                                <Border Background="#F5F7FA" BorderBrush="{StaticResource LightBorderBrush}" BorderThickness="1" CornerRadius="5" Padding="10,8" Margin="0,5,0,10"><TextBlock Text="{Binding SqlDatabaseName}" Foreground="{StaticResource RegularTextBrush}"/></Border>
                                <StackPanel Visibility="{Binding HasSqlParameters, Converter={StaticResource BoolVisibility}}">
                                    <DockPanel LastChildFill="False"><TextBlock Style="{StaticResource Label}" Text="查询参数"/><ComboBox ItemsSource="{Binding SqlParameterModes}" SelectedItem="{Binding SqlParameterMode}" Width="112" Margin="0,0,0,7" HorizontalAlignment="Right"/></DockPanel>
                                    <ScrollViewer MaxHeight="142" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                                        <ScrollViewer.Style><Style TargetType="ScrollViewer"><Setter Property="Visibility" Value="Collapsed"/><Style.Triggers><DataTrigger Binding="{Binding SqlParameterMode}" Value="逐项输入"><Setter Property="Visibility" Value="Visible"/></DataTrigger></Style.Triggers></Style></ScrollViewer.Style>
                                        <ItemsControl ItemsSource="{Binding SqlParameterInputs}">
                                            <ItemsControl.ItemTemplate><DataTemplate><Grid Margin="0,0,0,7"><Grid.ColumnDefinitions><ColumnDefinition Width="118"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><Border Background="#F5F7FA" BorderBrush="{StaticResource LightBorderBrush}" BorderThickness="1" CornerRadius="5,0,0,5" Padding="8,0"><TextBlock Text="{Binding Label}" VerticalAlignment="Center" Foreground="{StaticResource RegularTextBrush}" FontFamily="Consolas" FontSize="11" TextTrimming="CharacterEllipsis"/></Border><TextBox Grid.Column="1" Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}" Margin="-1,0,0,0" MinHeight="34" Padding="9,6"/></Grid></DataTemplate></ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </ScrollViewer>
                                    <TextBox Text="{Binding SqlParametersJson, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" Height="124" VerticalContentAlignment="Top" FontFamily="Consolas" FontSize="11" VerticalScrollBarVisibility="Auto">
                                        <TextBox.Style><Style TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}"><Setter Property="Visibility" Value="Collapsed"/><Setter Property="Margin" Value="0,0,0,8"/><Style.Triggers><DataTrigger Binding="{Binding SqlParameterMode}" Value="JSON"><Setter Property="Visibility" Value="Visible"/></DataTrigger></Style.Triggers></Style></TextBox.Style>
                                    </TextBox>
                                </StackPanel>
                                <TextBlock Text="使用真实数据库参数执行；仅允许单条 SELECT/WITH，最多返回 200 行。" Foreground="{StaticResource SecondaryTextBrush}" FontSize="10" TextWrapping="Wrap" Margin="0,5,0,12"/>
                                <Button Content="执行只读查询" Command="{Binding ExecuteSqlCommand}" Style="{StaticResource PrimaryButton}" HorizontalAlignment="Left"/>
                            </StackPanel>
                        </ScrollViewer>
                    </Grid>
                </Border>
            </Grid>
            <GridSplitter Grid.Row="3" Height="14" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" ResizeDirection="Rows" ResizeBehavior="PreviousAndNext" Background="Transparent" Cursor="SizeNS"><GridSplitter.Template><ControlTemplate TargetType="GridSplitter"><Border Background="Transparent"><Border Width="46" Height="4" CornerRadius="2" Background="#D8DEE8" HorizontalAlignment="Center" VerticalAlignment="Center"/></Border></ControlTemplate></GridSplitter.Template></GridSplitter>
            <Border Grid.Row="4" Style="{StaticResource Card}" Padding="0"><Grid><Grid.RowDefinitions><RowDefinition Height="54"/><RowDefinition Height="3"/><RowDefinition Height="*"/></Grid.RowDefinitions><Border BorderBrush="{StaticResource LightBorderBrush}" BorderThickness="0,0,0,1" Padding="16,0"><Grid><TextBlock Text="查询结果" Style="{StaticResource SectionTitle}" VerticalAlignment="Center"/><TextBlock Text="{Binding SqlResultSummary}" HorizontalAlignment="Right" VerticalAlignment="Center" Foreground="{StaticResource SecondaryTextBrush}" FontSize="10"/></Grid></Border><ProgressBar Grid.Row="1" IsIndeterminate="True" Visibility="{Binding IsSqlBusy, Converter={StaticResource BoolVisibility}}" Foreground="{StaticResource PrimaryBrush}" Height="3" BorderThickness="0"/><DataGrid Grid.Row="2" ItemsSource="{Binding SqlResultView}" AutoGenerateColumns="True" IsReadOnly="True" EnableRowVirtualization="True" EnableColumnVirtualization="True"/></Grid></Border>
        </Grid>

        <!-- 日志源配置 -->
        <Grid Grid.Column="1" Grid.Row="3" Margin="20" Visibility="{Binding IsLogConfigurationPage, Converter={StaticResource BoolVisibility}}">
            <Grid.ColumnDefinitions><ColumnDefinition Width="310"/><ColumnDefinition Width="14"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
            <Border Style="{StaticResource Card}" Padding="0"><Grid><Grid.RowDefinitions><RowDefinition Height="58"/><RowDefinition Height="*"/><RowDefinition Height="76"/></Grid.RowDefinitions><Border BorderBrush="{StaticResource LightBorderBrush}" BorderThickness="0,0,0,1" Padding="16,0"><StackPanel VerticalAlignment="Center"><TextBlock Text="日志源" Style="{StaticResource SectionTitle}"/><TextBlock Text="多个配置中仅启用一个" Foreground="{StaticResource SecondaryTextBrush}" FontSize="10" Margin="0,3,0,0"/></StackPanel></Border><ListBox Grid.Row="1" ItemsSource="{Binding LogProfiles}" SelectedItem="{Binding SelectedLogProfile}" BorderThickness="0" Background="Transparent" Margin="8"><ListBox.ItemTemplate><DataTemplate><Grid><Grid.ColumnDefinitions><ColumnDefinition Width="34"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><Border Width="28" Height="28" CornerRadius="6" Background="#ECF5FF"><TextBlock Text="文" Foreground="{StaticResource PrimaryBrush}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="11"/></Border><StackPanel Grid.Column="1" Margin="8,0,6,0"><TextBlock Text="{Binding Name}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/><TextBlock Text="{Binding RootDirectory}" Foreground="{StaticResource SecondaryTextBrush}" FontSize="10" TextTrimming="CharacterEllipsis" Margin="0,3,0,0"/></StackPanel><Border Grid.Column="2" Background="#F0F9EB" BorderBrush="#C2E7B0" BorderThickness="1" CornerRadius="9" Padding="6,2" VerticalAlignment="Center" Visibility="{Binding IsActive, Converter={StaticResource BoolVisibility}}"><TextBlock Text="使用中" Foreground="#529B2E" FontSize="9"/></Border></Grid></DataTemplate></ListBox.ItemTemplate></ListBox><StackPanel Grid.Row="2" Margin="12,7"><Button Content="设为当前使用" Command="{Binding ActivateLogProfileCommand}" CommandParameter="{Binding SelectedLogProfile}" Style="{StaticResource PrimaryButton}" Margin="0,0,0,7"/><Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><Button Content="＋ 新增" Command="{Binding NewLogCommand}"/><Button Grid.Column="1" Content="删除" Command="{Binding DeleteLogCommand}" Style="{StaticResource DangerButton}" Margin="4,0,0,0"/></Grid></StackPanel></Grid></Border>
            <Border Grid.Column="2" Style="{StaticResource Card}" Padding="0"><Grid><Grid.RowDefinitions><RowDefinition Height="58"/><RowDefinition Height="*"/></Grid.RowDefinitions><Border BorderBrush="{StaticResource LightBorderBrush}" BorderThickness="0,0,0,1" Padding="18,0"><StackPanel VerticalAlignment="Center"><TextBlock Text="日志源信息" Style="{StaticResource SectionTitle}"/><TextBlock Text="保存后可设为应用日志页面当前使用的日志源" Foreground="{StaticResource SecondaryTextBrush}" FontSize="10" Margin="0,3,0,0"/></StackPanel></Border><ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled"><Grid Margin="22,10,22,24" DataContext="{Binding LogEditor}" MaxWidth="820" HorizontalAlignment="Left"><Grid.ColumnDefinitions><ColumnDefinition Width="390"/><ColumnDefinition Width="24"/><ColumnDefinition Width="390"/></Grid.ColumnDefinitions><StackPanel><TextBlock Style="{StaticResource Label}" Text="配置名称 *"/><TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"/><TextBlock Style="{StaticResource Label}" Text="日志根目录 *"/><Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><TextBox Text="{Binding RootDirectory, UpdateSourceTrigger=PropertyChanged}"/><Button Grid.Column="1" Content="浏览" Click="BrowseLogDirectory_Click" Margin="8,5,0,12"/></Grid><TextBlock Style="{StaticResource Label}" Text="NLog.config（留空自动发现）"/><Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><TextBox Text="{Binding NLogConfigurationPath, UpdateSourceTrigger=PropertyChanged}"/><Button Grid.Column="1" Content="浏览" Click="BrowseNLogConfig_Click" Margin="8,5,0,12"/></Grid><TextBlock Style="{StaticResource Label}" Text="File Target 名称"/><TextBox Text="{Binding NLogTargetName}"/><TextBlock Style="{StaticResource Label}" Text="关联数据库（用于 SQL 只读试查）"/><ComboBox ItemsSource="{Binding DataContext.DatabaseProfiles, RelativeSource={RelativeSource AncestorType=Window}}" DisplayMemberPath="Name" SelectedValuePath="Id" SelectedValue="{Binding DatabaseProfileId}"/></StackPanel><StackPanel Grid.Column="2"><TextBlock Style="{StaticResource Label}" Text="文件名模式"/><TextBox Text="{Binding FilePattern}"/><TextBlock Style="{StaticResource Label}" Text="编码（如 GB18030）"/><TextBox Text="{Binding EncodingName}"/><TextBlock Style="{StaticResource Label}" Text="单文件读取上限（1–10 MB）"/><TextBox Text="{Binding MaximumReadMegabytesPerFile}"/><TextBlock Style="{StaticResource Label}" Text="NLog Layout 覆盖"/><TextBox Text="{Binding LayoutOverride}" AcceptsReturn="True" Height="112" TextWrapping="Wrap" VerticalContentAlignment="Top"/><Border Background="#ECF5FF" BorderBrush="#D9ECFF" BorderThickness="1" CornerRadius="6" Padding="11" Margin="0,0,0,16"><TextBlock Text="日志目录会递归扫描日期子文件夹；超过大小上限的文件将被跳过。" Foreground="#337ECC" TextWrapping="Wrap" FontSize="10"/></Border><Button Content="保存日志源" Command="{Binding DataContext.SaveLogCommand, RelativeSource={RelativeSource AncestorType=Window}}" Style="{StaticResource PrimaryButton}" HorizontalAlignment="Left"/></StackPanel></Grid></ScrollViewer></Grid></Border>
        </Grid>

        <!-- 数据库配置 -->
        <Grid Grid.Column="1" Grid.Row="3" Margin="20" Visibility="{Binding IsDatabaseConfigurationPage, Converter={StaticResource BoolVisibility}}">
            <Grid.ColumnDefinitions><ColumnDefinition Width="310"/><ColumnDefinition Width="14"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
            <Border Style="{StaticResource Card}" Padding="0"><Grid><Grid.RowDefinitions><RowDefinition Height="58"/><RowDefinition Height="*"/><RowDefinition Height="58"/></Grid.RowDefinitions><Border BorderBrush="{StaticResource LightBorderBrush}" BorderThickness="0,0,0,1" Padding="16,0"><StackPanel VerticalAlignment="Center"><TextBlock Text="数据库连接" Style="{StaticResource SectionTitle}"/><TextBlock Text="凭据由当前 Windows 用户保护" Foreground="{StaticResource SecondaryTextBrush}" FontSize="10" Margin="0,3,0,0"/></StackPanel></Border><ListBox Grid.Row="1" ItemsSource="{Binding DatabaseProfiles}" SelectedItem="{Binding SelectedDatabaseProfile}" BorderThickness="0" Background="Transparent" Margin="8"><ListBox.ItemTemplate><DataTemplate><Grid><Grid.ColumnDefinitions><ColumnDefinition Width="34"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><Border Width="28" Height="28" CornerRadius="6" Background="#F0F9EB"><TextBlock Text="库" Foreground="#67C23A" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="11"/></Border><StackPanel Grid.Column="1" Margin="8,0,0,0"><TextBlock Text="{Binding Name}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/><TextBlock Text="{Binding Database}" Foreground="{StaticResource SecondaryTextBrush}" FontSize="10" TextTrimming="CharacterEllipsis" Margin="0,3,0,0"/></StackPanel></Grid></DataTemplate></ListBox.ItemTemplate></ListBox><Grid Grid.Row="2" Margin="12"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><Button Content="＋ 新增" Command="{Binding NewDatabaseCommand}" Style="{StaticResource PrimaryButton}"/><Button Grid.Column="1" Content="删除" Command="{Binding DeleteDatabaseCommand}" Style="{StaticResource DangerButton}" Margin="4,0,0,0"/></Grid></Grid></Border>
            <Border Grid.Column="2" Style="{StaticResource Card}" Padding="0"><Grid><Grid.RowDefinitions><RowDefinition Height="58"/><RowDefinition Height="*"/></Grid.RowDefinitions><Border BorderBrush="{StaticResource LightBorderBrush}" BorderThickness="0,0,0,1" Padding="18,0"><StackPanel VerticalAlignment="Center"><TextBlock Text="连接信息" Style="{StaticResource SectionTitle}"/><TextBlock Text="录入完成后自动加密保存" Foreground="{StaticResource SecondaryTextBrush}" FontSize="10" Margin="0,3,0,0"/></StackPanel></Border><ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled"><Grid Margin="22,10,22,24" DataContext="{Binding DatabaseEditor}" MaxWidth="820" HorizontalAlignment="Left"><Grid.ColumnDefinitions><ColumnDefinition Width="390"/><ColumnDefinition Width="24"/><ColumnDefinition Width="390"/></Grid.ColumnDefinitions><StackPanel><TextBlock Style="{StaticResource Label}" Text="配置名称 *"/><TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"/><TextBlock Style="{StaticResource Label}" Text="数据库类型"/><ComboBox ItemsSource="{Binding DataContext.Providers, RelativeSource={RelativeSource AncestorType=Window}}" DisplayMemberPath="Name" SelectedValuePath="InvariantName" SelectedValue="{Binding ProviderInvariantName}" SelectionChanged="Provider_SelectionChanged"/><Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="14"/><ColumnDefinition Width="90"/></Grid.ColumnDefinitions><StackPanel><TextBlock Style="{StaticResource Label}" Text="主机 / 文件路径"/><TextBox Text="{Binding Host}"/></StackPanel><StackPanel Grid.Column="2"><TextBlock Style="{StaticResource Label}" Text="端口"/><TextBox Text="{Binding Port}"/></StackPanel></Grid><TextBlock Style="{StaticResource Label}" Text="数据库名（SQLite 填文件路径）"/><TextBox Text="{Binding Database}"/></StackPanel><StackPanel Grid.Column="2"><TextBlock Style="{StaticResource Label}" Text="用户名"/><TextBox Text="{Binding UserName}"/><TextBlock Style="{StaticResource Label}" Text="密码（留空保留原密码）"/><PasswordBox x:Name="DatabasePasswordBox" PasswordChanged="DatabasePasswordBox_PasswordChanged"/><TextBlock Style="{StaticResource Label}" Text="连接超时（秒）"/><TextBox Text="{Binding ConnectionTimeoutSeconds}"/><CheckBox Content="使用 Windows 集成认证（SQL Server）" IsChecked="{Binding IntegratedSecurity}" Margin="0,2,0,16" Foreground="{StaticResource RegularTextBrush}"/><Border Background="#F0F9EB" BorderBrush="#E1F3D8" BorderThickness="1" CornerRadius="6" Padding="11" Margin="0,0,0,16"><TextBlock Text="密码使用 Windows DPAPI / CurrentUser 加密，配置文件不会保存明文。" Foreground="#529B2E" TextWrapping="Wrap" FontSize="10"/></Border><StackPanel Orientation="Horizontal"><Button Content="保存连接" Click="SaveDatabase_Click" Style="{StaticResource PrimaryButton}"/><Button Content="测试连接" Click="TestDatabase_Click" Margin="0"/></StackPanel></StackPanel></Grid></ScrollViewer></Grid></Border>
        </Grid>

        <Border Grid.Column="1" Grid.Row="4" Background="#FAFBFC" BorderBrush="{StaticResource LightBorderBrush}" BorderThickness="0,1,0,0"><Grid Margin="20,0"><Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><Ellipse Width="7" Height="7" Fill="{StaticResource SuccessBrush}" VerticalAlignment="Center"/><TextBlock Grid.Column="1" Text="{Binding StatusText}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" Margin="7,0,12,0" FontSize="11" Foreground="{StaticResource RegularTextBrush}"/><TextBlock Grid.Column="2" Text="AiDataGateway · 本机安全模式" VerticalAlignment="Center" FontSize="9.5" Foreground="{StaticResource SecondaryTextBrush}"/></Grid></Border>

        <!-- Element 风格应用内弹窗 -->
        <Border Grid.ColumnSpan="2" Grid.RowSpan="5" Panel.ZIndex="100" Background="#66000000" Visibility="{Binding IsDialogOpen, Converter={StaticResource BoolVisibility}}">
            <Border Width="440" MinHeight="210" MaxHeight="430" Background="White" CornerRadius="9" HorizontalAlignment="Center" VerticalAlignment="Center"><Border.Effect><DropShadowEffect BlurRadius="28" ShadowDepth="8" Opacity="0.28"/></Border.Effect><Grid><Grid.RowDefinitions><RowDefinition Height="60"/><RowDefinition Height="*"/><RowDefinition Height="64"/></Grid.RowDefinitions><Border BorderBrush="{StaticResource LightBorderBrush}" BorderThickness="0,0,0,1" Padding="20,0"><Grid><TextBlock Text="{Binding DialogTitle}" FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center"/><Button HorizontalAlignment="Right" Content="×" Command="{Binding CancelDialogCommand}" Style="{StaticResource TextButton}" Foreground="{StaticResource SecondaryTextBrush}" FontSize="20" Margin="0"/></Grid></Border><ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto"><Grid Margin="20,18"><Grid.ColumnDefinitions><ColumnDefinition Width="38"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><Border Width="30" Height="30" CornerRadius="15" Background="{StaticResource PrimarySoftBrush}" VerticalAlignment="Top"><TextBlock Text="i" Foreground="{StaticResource PrimaryBrush}" FontSize="17" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/></Border><TextBlock Grid.Column="1" Text="{Binding DialogMessage}" TextWrapping="Wrap" Foreground="{StaticResource RegularTextBrush}" LineHeight="23" Margin="8,3,0,0"/></Grid></ScrollViewer><Border Grid.Row="2" Background="#FAFAFC" BorderBrush="{StaticResource LightBorderBrush}" BorderThickness="0,1,0,0" Padding="16,0"><StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center"><Button Content="取消" Command="{Binding CancelDialogCommand}" Visibility="{Binding IsDialogConfirmation, Converter={StaticResource BoolVisibility}}"/><Button Content="{Binding DialogConfirmText}" Command="{Binding ConfirmDialogCommand}" Style="{StaticResource PrimaryButton}" Margin="0"/></StackPanel></Border></Grid></Border>
        </Border>
    </Grid>
</Window>
