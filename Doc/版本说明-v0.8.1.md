# AiDataGateway v0.8.1 版本说明

## 本次更新

- 修复“检查更新”在 GitHub API 限流时失败的问题：遇到 403/429 限流自动回退到 releases 订阅源（普通 github.com 通道，不受每小时 60 次的 API 配额限制）。
- 回退通道从订阅源解析最新版本号，并按标签直接构造安装器与 SHA256 校验文件的下载地址。

## 兼容与升级

- 不修改 `gateway.db` 结构，无需重新初始化数据库。
- 已安装 v0.8.0 的用户可在配额恢复后通过自动更新收到本版本，或直接从 Releases 页下载安装器覆盖安装。
