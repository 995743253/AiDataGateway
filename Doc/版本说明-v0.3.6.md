# AiDataGateway v0.3.6 版本说明

## 本次更新

- 历史趋势改为按趋势点顺序均匀铺开，解决数据因采集空档集中在图表单点附近的问题。
- 历史趋势不再显示采样圆点及节点悬浮详情，减少大量历史数据造成的视觉堆叠。
- 增加最高值和最低值虚线，并直接显示格式化后的极值；当两者相同时合并为一条虚线。
- 近期趋势继续支持节点悬浮详情，但缩小可见圆点及描边，保留较大的透明命中区域以兼顾易用性。

## 兼容与升级

- 本次只修改前端趋势图逻辑，不修改数据库结构和接口，无需重新初始化 `gateway.db`。
