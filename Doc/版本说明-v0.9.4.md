# AiDataGateway v0.9.4 版本说明

## 本次更新

- 修复工具箱“WebHook 调试”页面打不开的问题：SQLite 不支持对 DateTimeOffset 列排序，WebHook 列表改为在内存中按创建时间排序（与审批等既有模块一致）。

## 兼容与升级

- 不修改 `gateway.db` 结构，无需重新初始化数据库。
- 已安装 v0.9.3 的用户会通过内置自动更新收到本版本。
