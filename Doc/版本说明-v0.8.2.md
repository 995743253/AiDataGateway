# AiDataGateway v0.8.2 版本说明

## 本次更新

- 修复所有列表页分页条不显示的问题（v0.8.0 重构分页时漏注册了分页组件）。
- 检查到新版本后不再直接弹窗，改为在主窗口底栏显示“立即更新到 vX.Y.Z”按钮，点击后开始下载并自动升级；下载进度显示在状态栏。

## 兼容与升级

- 不修改 `gateway.db` 结构，无需重新初始化数据库。
- 已安装 v0.8.0 / v0.8.1 的用户会通过自动更新收到本版本。
