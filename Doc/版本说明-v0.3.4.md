# AiDataGateway v0.3.4 版本说明

## 本次更新

- 趋势图改为固定比例等比缩放，解决内嵌窗口尺寸变化时文字、圆点和折线被拉伸的问题。
- 节点 X 位置按真实采集时间计算，不再把不等间隔的样本机械地平均铺满图表。
- 增加轻量面积渐变，在不影响坐标轴与节点读取的前提下改善趋势层次。
- 保留 X/Y 轴、悬浮节点详情、近期趋势和历史时间范围查询。

## 兼容与升级

- 本次仅修改前端绘图，不修改数据库结构和接口，无需重新初始化 `gateway.db`。
